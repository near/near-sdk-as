{
  "name": "nearcore-tests",
  "version": "2.2.1",
  "scripts": {
    "pretest": "yarn build",
    "test": "./test.sh",
    "build": "yarn asb ../sdk/assembly/__tests__/main.ts"
  },
  "private": true,
  "devDependencies": {
    "near-sdk-as": "^2.2.1"
  }
}
