{
  "name": "near-sdk-sim",
  "version": "0.1.0",
  "description": "Mock Near Runtime for integration testing.",
  "license": "(MIT AND Apache-2.0)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "author": "Willem Wyndham <willem@nearprotocol.com>",
  "scripts": {
    "build:release": "node asconfig.js",
    "build:debug": "node asconfig.js --target debug --wat",
    "build": "yarn build:tsc && yarn build:debug",
    "test": "yarn jest",
    "build:tsc": "tsc",
    "pretest": "yarn build:debug",
    "prepublishOnly": "yarn build:tsc"
  },
  "devDependencies": {
    "near-sdk-core": "^3.1.0",
    "sim-ffi": "0.1.0"
  }
}
