{
  "name": "near-bindgen",
  "version": "0.0.0",
  "description": "Tests for near bindings",
  "main": "test.js",
  "scripts": {
    "build": "../node_modules/.bin/asc assembly/test.ts --transform ../ --validate --runtime stub --binaryFile out/test.wasm --textFile out/test.wat --runPasses dce --debug",
    "test": "npm run build:asconfig && npm run build && node --experimental-wasm-bigint ./test.js",
    "build:asconfig": "node asconfig.js"
  },
  "dependencies": {}
}
