{
  "scripts": {
    "test": "node --experimental-wasm-bigint ../node_modules/.bin/asp",
    "build": "mkdir -p out && gulp"
  },
  "dependencies": {
    "near-runtime-ts": "file:.."
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "near-shell": "^0.10.4"
  }
}
