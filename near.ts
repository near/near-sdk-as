//temporary hack to work around import { storage, near, base64 } from "./near in intermediate main.near.ts files. It doesn't look like this is actually used
export namespace base64 {}
export namespace storage {}
export namespace near {}
